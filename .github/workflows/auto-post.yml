name: Auto Generate Blog Post

on:
  schedule:
    # v8.2 (2026-06-04): 저녁 시간대(KST 18~22시) 랜덤 발행.
    # v13 (2026-07-28): 하루 정확히 1편. 아래 5개 슬롯이 매일 모두 트리거되지만,
    # gate step 이 그 중 1개만 확률적으로 선택해 발행한다. 발행 시각만 랜덤이고
    # 편수는 고정 — "매일 같은 시각 발행" 패턴은 계속 회피.
    - cron: '11 9 * * *'   # ~KST 18:11
    - cron: '23 10 * * *'  # ~KST 19:23
    - cron: '42 11 * * *'  # ~KST 20:42
    - cron: '17 12 * * *'  # ~KST 21:17
    - cron: '50 12 * * *'  # ~KST 21:50
  workflow_dispatch: # Allow manual trigger
    inputs:
      count:
        description: 'number of posts to generate in one run'
        required: false
        default: '1'

permissions:
  contents: write
  pages: write
  id-token: write

# 동시 실행 직렬화 — 슬롯 경합으로 인한 push 충돌/리베이스 깨짐 방지 (2026-05-30)
concurrency:
  group: auto-post-${{ github.ref }}
  cancel-in-progress: false

jobs:
  generate-post:
    runs-on: ubuntu-latest
    outputs:
      post: ${{ steps.gate.outputs.post }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Gate — pick one random evening slot per day
        id: gate
        run: |
          # 수동 트리거(workflow_dispatch)는 항상 발행
          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "manual/dispatch run — post"
            echo "post=true" >> "$GITHUB_OUTPUT"; exit 0
          fi
          # 하루 발행 목표 편수. 오늘(UTC) 이미 채웠으면 skip — 하루 정확히 N편 보장.
          # ⚠️ 카운트 기준 UTC: 슬롯(cron)·커밋 타임스탬프가 UTC인데 KST(+09:00)로 세면
          #    GitHub Actions 스케줄 지연(3~6h)이 KST 자정 경계(UTC 15:00)를 넘겨, 전날 발행을
          #    오늘로 잘못 세 하루 전체 skip되는 버그가 났었다(2026-06-06 0편). UTC로 통일.
          TODAY_UTC="$(date -u +%Y-%m-%d)"
          # v13 (2026-07-28, 쿠마님 지시): 하루 정확히 1편.
          # v12 의 1/2/3편 랜덤(평균 2편)에서 1편 고정으로 축소 — 편수를 줄이고 글 하나에 집중.
          # 규칙성 회피는 발행 '시각' 랜덤(5슬롯 중 1개 + 0~30분 지터)이 계속 담당한다.
          DAILY_TARGET=1
          # 발행수는 최신 원격(origin) 기준으로 센다 — 직렬 대기하다 시작한 run 이 트리거
          # 시점의 stale SHA 를 checkout 해 앞 슬롯 발행을 못 보고 과발행(3편+)하는 것 방지.
          # fetch 실패 시 로컬 HEAD 로 안전 폴백(기존 동작 유지).
          git fetch origin "${{ github.ref_name }}" --quiet 2>/dev/null || true
          COUNT_REF="origin/${{ github.ref_name }}"
          git rev-parse --verify --quiet "$COUNT_REF" >/dev/null || COUNT_REF=HEAD
          # git log 자체 실패는 fail-closed(발행 안 함). `| grep -c ... || true` 한 줄로 쓰면
          # git log 실패까지 || true 가 삼켜 published=0 → 과발행하므로, log 단계를 분리해
          # 실패 시 이 슬롯은 skip. grep 0매치(빈 날)만 || true 로 정상 0 처리.
          # v17 (2026-08-19, 쿠마님 지시): 손으로 미리 채워 둔 날은 봇이 덧쓰지 않는다.
          # 오늘 날짜 파일(_posts/YYYY-MM-DD-*.md)이 이미 있으면 그 날은 발행 완료로 본다.
          # (커밋 메시지 카운트만 보면, 며칠치를 한 번에 커밋한 다음 날 봇이 또 써서 2편이 된다.)
          if git ls-tree --name-only "$COUNT_REF" _posts/ 2>/dev/null | grep -q "^_posts/${TODAY_UTC}-"; then
            echo "a post file for $TODAY_UTC already exists (pre-written) — skip"
            echo "post=false" >> "$GITHUB_OUTPUT"; exit 0
          fi
          if ! log_subjects=$(git log "$COUNT_REF" --since="${TODAY_UTC}T00:00:00Z" --pretty=%s); then
            echo "git log failed — skip this slot for safety"
            echo "post=false" >> "$GITHUB_OUTPUT"; exit 0
          fi
          # v12: 새 커밋 프리픽스('Add post:')와 과거 프리픽스('Auto-publish new') 둘 다 카운트(전환일 안전)
          published=$(printf '%s\n' "$log_subjects" | grep -cE '^(Auto-publish new|Add post:)' || true)
          need=$(( DAILY_TARGET - published ))
          if [ "$need" -le 0 ]; then
            echo "already published $published today (UTC $TODAY_UTC, target $DAILY_TARGET) — skip"
            echo "post=false" >> "$GITHUB_OUTPUT"; exit 0
          fi
          # 슬롯 균등 확률: 남은 슬롯에 '남은 발행 수(need)'를 균등 분산.
          # 현재 슬롯 당첨 확률 = need/remaining. 남은 슬롯이 need 이하로 줄면 강제 발행.
          # → 하루 DAILY_TARGET 편이 5개 슬롯에 균등하게 흩어짐.
          SLOTS=("11 9 * * *" "23 10 * * *" "42 11 * * *" "17 12 * * *" "50 12 * * *")
          THIS="${{ github.event.schedule }}"
          idx=-1
          for i in "${!SLOTS[@]}"; do [ "${SLOTS[$i]}" = "$THIS" ] && idx=$i; done
          if [ "$idx" -lt 0 ]; then
            echo "unrecognized slot ($THIS) — post to be safe"
            echo "post=true" >> "$GITHUB_OUTPUT"; exit 0
          fi
          remaining=$(( ${#SLOTS[@]} - idx ))
          if [ "$(( RANDOM % remaining ))" -lt "$need" ]; then
            echo "slot #$idx chosen (need $need / remaining $remaining, published $published) — post"
            echo "post=true" >> "$GITHUB_OUTPUT"
          else
            echo "slot #$idx skipped (need $need / remaining $remaining, published $published) — wait for a later slot"
            echo "post=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python
        if: steps.gate.outputs.post == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Random delay (0-1800s) — extra jitter within the slot
        if: steps.gate.outputs.post == 'true' && github.event_name == 'schedule'
        run: |
          DELAY=$((RANDOM % 1800))
          echo "Sleeping ${DELAY}s before generation"
          sleep $DELAY

      - name: Install dependencies
        if: steps.gate.outputs.post == 'true'
        run: pip install openai Pillow

      - name: Generate new post (with retry on transient failures)
        if: steps.gate.outputs.post == 'true'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 28
          max_attempts: 3
          retry_wait_seconds: 60
          command: python scripts/generate_post.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          POST_COUNT: ${{ github.event.inputs.count || '1' }}

      - name: Commit new post
        if: steps.gate.outputs.post == 'true'
        run: |
          git config user.name "SmartMoneyDaily Bot"
          git config user.email "bot@smartmoneydaily.com"
          git add _posts/ scripts/used_topics.json scripts/used_promo_titles.json assets/pin-images/
          if git diff --staged --quiet; then
            echo "no staged changes"
            exit 0
          fi
          # v12: 커밋 메시지의 자동화 표식 완화 (공개 리포 — 'Auto-publish'가 이력에 그대로 노출됐었음)
          git commit -m "Add post: $(date +%Y-%m-%d-%H%M)"
          for i in 1 2 3 4 5; do
            if git push; then echo pushed; exit 0; fi
            echo "push attempt $i failed; aborting half-done rebase and re-syncing"
            git rebase --abort 2>/dev/null || true
            git pull --rebase -X ours origin main || { git rebase --abort 2>/dev/null || true; sleep $((RANDOM % 10 + 3)); }
          done
          echo "push failed after retries"; exit 1

      - name: Setup Ruby
        if: steps.gate.outputs.post == 'true'
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Setup Pages
        id: pages
        if: steps.gate.outputs.post == 'true'
        uses: actions/configure-pages@v4

      - name: Build with Jekyll
        if: steps.gate.outputs.post == 'true'
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        if: steps.gate.outputs.post == 'true'
        uses: actions/upload-pages-artifact@v3

  deploy:
    needs: generate-post
    if: needs.generate-post.outputs.post == 'true'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout for IndexNow
        uses: actions/checkout@v4
        with:
          # ref 없이 체크아웃하면 워크플로 '트리거 시점' SHA 를 받는다. 새 글 커밋은 그
          # 이후 generate-post 잡에서 만들어지므로 작업트리에 없고, 아래 IndexNow/Pinterest
          # 스텝의 `git log --since '1 hour ago' --diff-filter=A` 가 항상 빈 결과가 됐다
          # (실측: 로그가 매번 "IndexNow: no new URLs in last hour"). 브랜치 최신 헤드를 받게 고정.
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      - name: IndexNow ping (Bing/Yandex/Naver)
        if: success()
        continue-on-error: true
        run: |
          python3 - <<'PY'
          import json, os, subprocess, urllib.request, re
          repo = os.environ.get("GITHUB_REPOSITORY","").split("/")[-1]
          if not repo:
              raise SystemExit(0)
          since_iso = subprocess.check_output(
              ["bash","-c","date -u -d '1 hour ago' +'%Y-%m-%dT%H:%M:%SZ'"]
          ).decode().strip()
          out = subprocess.check_output(
              ["git","log","--since",since_iso,"--name-only","--pretty=format:","--diff-filter=A"]
          ).decode()
          urls = []
          for line in out.split("\n"):
              m = re.match(r"_posts/(\d{4})-(\d{2})-(\d{2})-(.+)\.md$", line.strip())
              if m:
                  yyyy, mm, dd, slug = m.groups()
                  urls.append(f"https://smartmoneydaily.github.io/{yyyy}/{mm}/{dd}/{slug}/")
          if not urls:
              print("IndexNow: no new URLs in last hour")
              raise SystemExit(0)
          payload = {
              "host": "smartmoneydaily.github.io",
              "key": "3edb9ac60100921ec4665d794fb30755",
              "keyLocation": "https://smartmoneydaily.github.io/3edb9ac60100921ec4665d794fb30755.txt",
              "urlList": urls,
          }
          req = urllib.request.Request(
              "https://api.indexnow.org/IndexNow",
              data=json.dumps(payload).encode(),
              headers={"Content-Type":"application/json"},
              method="POST",
          )
          try:
              resp = urllib.request.urlopen(req, timeout=15)
              print(f"IndexNow OK: {resp.status} ({len(urls)} URLs)")
          except Exception as e:
              print(f"IndexNow fail (non-fatal): {e}")
          PY

      - name: Pinterest auto-pin (new posts)
        if: success()
        continue-on-error: true
        run: |
          python3 - <<'PY'
          import json, os, subprocess, urllib.request, urllib.parse, urllib.error, base64, re
          repo = os.environ.get("GITHUB_REPOSITORY","").split("/")[-1]
          if not repo:
              raise SystemExit(0)
          since_iso = subprocess.check_output(
              ["bash","-c","date -u -d '1 hour ago' +'%Y-%m-%dT%H:%M:%SZ'"]
          ).decode().strip()
          out = subprocess.check_output(
              ["git","log","--since",since_iso,"--name-only","--pretty=format:","--diff-filter=A"]
          ).decode()
          new_posts = []
          for line in out.split("\n"):
              line = line.strip()
              if line.startswith("_posts/") and line.endswith(".md"):
                  new_posts.append(line)
          if not new_posts:
              print("Pinterest: no new posts in last hour")
              raise SystemExit(0)
          print(f"Pinterest: found {len(new_posts)} new post(s)")

          # refresh_token으로 access_token 즉시 갱신
          refresh = os.environ["PINTEREST_REFRESH_TOKEN"]
          app_id = os.environ["PINTEREST_APP_ID"]
          app_secret = os.environ["PINTEREST_APP_SECRET"]
          board_id = os.environ["PINTEREST_BOARD_ID"]
          creds = base64.b64encode(f"{app_id}:{app_secret}".encode()).decode()
          body = urllib.parse.urlencode({
              "grant_type": "refresh_token",
              "refresh_token": refresh,
          }).encode()
          token_req = urllib.request.Request(
              "https://api.pinterest.com/v5/oauth/token",
              data=body,
              headers={
                  "Authorization": f"Basic {creds}",
                  "Content-Type": "application/x-www-form-urlencoded",
              },
              method="POST",
          )
          try:
              token_resp = urllib.request.urlopen(token_req, timeout=20)
              tokens = json.loads(token_resp.read())
              access = tokens["access_token"]
              print(f"Pinterest: access_token refreshed (expires_in {tokens.get('expires_in')}s)")
          except urllib.error.HTTPError as e:
              detail = e.read().decode()[:200]
              print(f"Pinterest token refresh FAIL HTTP {e.code}: {detail}")
              # refresh_token 은 60일 만료다. 만료를 모르고 19일간 핀 0개로 방치된 적이 있어
              # (2026-07-08 만료 → 07-28 발견) 갱신 실패는 반드시 텔레그램으로 알린다.
              tg_token = os.environ.get("TELEGRAM_BOT_TOKEN", "")
              tg_chat = os.environ.get("TELEGRAM_CHAT_ID", "")
              if tg_token and tg_chat:
                  msg = (
                      "⚠️ 핀터레스트 자동 등록 중단\n"
                      f"토큰 갱신 실패 (HTTP {e.code}). refresh_token 만료로 보입니다.\n"
                      "재발급: 블로그 클로드/pinterest_oauth_helper.py 실행 → GitHub Secret 갱신"
                  )
                  try:
                      urllib.request.urlopen(
                          f"https://api.telegram.org/bot{tg_token}/sendMessage",
                          data=urllib.parse.urlencode({"chat_id": tg_chat, "text": msg}).encode(),
                          timeout=15,
                      )
                  except Exception as te:
                      print(f"  telegram notify failed: {te}")
              raise SystemExit(0)

          # 각 새 글 마다 핀 업로드
          for post_path in new_posts:
              m = re.match(r"_posts/(\d{4})-(\d{2})-(\d{2})-(.+)\.md$", post_path)
              if not m:
                  continue
              yyyy, mm, dd, slug = m.groups()
              page_url = f"https://smartmoneydaily.github.io/{yyyy}/{mm}/{dd}/{slug}/"
              pin_image = f"https://smartmoneydaily.github.io/assets/pin-images/{yyyy}-{mm}-{dd}-{slug}.png"
              try:
                  with open(post_path, encoding="utf-8") as f:
                      content = f.read()
              except Exception as e:
                  print(f"  read {post_path} fail: {e}")
                  continue
              fm = re.search(r"---\n(.*?)\n---", content, re.DOTALL)
              if fm:
                  title_m = re.search(r'title:\s*"(.+?)"', fm.group(1))
                  desc_m = re.search(r'description:\s*"(.+?)"', fm.group(1))
                  title = title_m.group(1) if title_m else slug.replace("-"," ").title()
                  description = desc_m.group(1) if desc_m else ""
              else:
                  title, description = slug.replace("-"," ").title(), ""
              pin_body = {
                  "board_id": board_id,
                  "title": title[:100],
                  "description": description[:500],
                  "link": page_url,
                  "media_source": {
                      "source_type": "image_url",
                      "url": pin_image,
                  },
              }
              pin_req = urllib.request.Request(
                  "https://api.pinterest.com/v5/pins",
                  data=json.dumps(pin_body).encode(),
                  headers={
                      "Authorization": f"Bearer {access}",
                      "Content-Type": "application/json",
                  },
                  method="POST",
              )
              try:
                  pin_resp = urllib.request.urlopen(pin_req, timeout=30)
                  pin_data = json.loads(pin_resp.read())
                  print(f"  Pin OK: id={pin_data.get('id')} title={title[:60]}")
              except urllib.error.HTTPError as e:
                  print(f"  Pin FAIL ({title[:30]}): HTTP {e.code} {e.read().decode()[:300]}")
              except Exception as e:
                  print(f"  Pin FAIL ({title[:30]}): {e}")
          PY
        env:
          PINTEREST_REFRESH_TOKEN: ${{ secrets.PINTEREST_REFRESH_TOKEN }}
          PINTEREST_APP_ID: ${{ secrets.PINTEREST_APP_ID }}
          PINTEREST_APP_SECRET: ${{ secrets.PINTEREST_APP_SECRET }}
          PINTEREST_BOARD_ID: ${{ secrets.PINTEREST_BOARD_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
